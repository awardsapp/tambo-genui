---
title: Genui MCP Server
description: Learn how to set up the Genui MCP server in various development environments.
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";

Genui's MCP server provides tools for an LLM to retrieve information from our documentation about how to use Genui. Add our MCP server to your IDE to give your coding assistant knowledge of how to use Genui.

## Quick Setup

The fastest way to add genui MCP to Cursor is by clicking the "Add to IDE" button on our homepage and selecting your preferred IDE. For other IDEs or manual setup, follow the instructions below.

## Setup Instructions

<Tabs items={['Cursor', 'Claude', 'VSCode', 'Windsurf', 'Zed']}>
<Tab id="tab-cursor" value="Cursor">
1. Click the install button on the homepage, or go directly to your global MCP configuration file at `~/.cursor/mcp.json`.
2. Add the following configuration:
```bash
"mcpServers": {
    "genui": {
        "url": "https://mcp.genui.co/mcp"
    },
}
```
4. Save the file
5. Restart Cursor
</Tab>

<Tab id="tab-claude" value="Claude">

**Claude Desktop**

1. Open Claude Desktop
2. Go to Settings (from the Claude menu) → Developer → Edit Config
3. Add the following configuration:

```bash
"mcpServers": {
  "genui": {
    "url": "https://mcp.genui.co/mcp"
  },
}
```

    4. Save the file
    5. Restart Claude Desktop

**Claude Code**

```bash
claude mcp add --transport sse genui-server https://mcp.genui.co/mcp
```

  </Tab>

<Tab id="tab-vscode" value="VSCode">
  1. Go to your global MCP configuration file at `.vscode/mcp.json`.
  2. Add the following configuration:
  ```bash 
  "servers": {
    "genui": {
      "type": "http",
      "url": "https://mcp.genui.co/mcp"
    }
  }
  ``` 
  4. Save the file
  5. Restart VSCode
  6. Alternatively, run the **MCP: Add Server** command from the Command Palette, choose the type of MCP server to add and provide the server information. Next, select Workspace Settings to create the `.vscode/mcp.json` file in your workspace if it doesn't already exist
</Tab>

<Tab id="tab-windsurf" value="Windsurf">
  1. Click the hammer icon (🔨) in Cascade
  2. Click Configure to open ~/.codeium/windsurf/mcp_config.json
  3. Add the following configuration: 
  ```bash 
  "mcpServers": {
    "genui": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.genui.co/mcp"]
    },
  }
  ``` 
  4. Save the file
  5. Click Refresh (🔄) in the MCP toolbar
</Tab>

<Tab id="tab-zed" value="Zed">
1. Open settings with `Cmd + ,`
2. Add the following configuration:
```json
{
    "context_servers": {
    "genui": {
        "command": {
        "path": "npx",
        "args": ["-y", "mcp-remote", "https://mcp.genui.co/mcp"],
        "env": {}
        },
        "settings": {}
    }
    }
}
```
</Tab>
</Tabs>

## Verification

After setting up the MCP server, you can verify it's working:

1. Open a new chat or file
2. Try asking the AI assistant about genui
3. Check the IDE's output/console for any error messages
