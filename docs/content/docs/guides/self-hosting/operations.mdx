---
title: Operations
description: Back up, restore, upgrade, and monitor your self-hosted Genui deployment.
---

Procedures for day-to-day management of a self-hosted Genui deployment. For a list of all available scripts, see the [Scripts reference](/guides/self-hosting/scripts).

## Back up the database

```bash
docker compose --env-file docker.env exec postgres pg_dump -U postgres genui > backup.sql
```

## Restore from backup

```bash
docker compose --env-file docker.env exec -T postgres psql -U postgres genui < backup.sql
```

## Connect to PostgreSQL

```bash
./scripts/cloud/genui-psql.sh

# Or via docker compose directly
docker compose --env-file docker.env exec postgres psql -U postgres -d genui
```

## Check service health

All services include Docker health checks. Check status:

```bash
docker compose --env-file docker.env ps
```

## Upgrade to a new version

1. Pull latest changes: `git pull`
2. Rebuild images: `./scripts/cloud/genui-build.sh`
3. Restart services: `./scripts/cloud/genui-stop.sh && ./scripts/cloud/genui-start.sh`
4. Run migrations: `./scripts/cloud/init-database.sh`
